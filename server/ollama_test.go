package server

import "testing"

func TestOllamaResponse(t *testing.T) {
    result := "expected output" // replace with an actual function call
    if result == "" {
        t.Error("Expected a response but got empty string")
    }
}